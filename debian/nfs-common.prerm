#!/bin/sh

set -e
set -x

cat /var/lib/nfs/etab || true
exportfs -s || true
showmount -e || true
#DEBHELPER#
showmount -e || true
exportfs -s || true
cat /var/lib/nfs/etab || true

if [ "$1" = remove ]
then
    rm -f /var/lib/nfs/sm/*      \
          /var/lib/nfs/sm.bak/*  \
          /var/lib/nfs/state
fi
